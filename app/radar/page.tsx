import PlaceholderPage from '@/components/placeholder-page';

export default function RadarPage() {
  return (
    <PlaceholderPage
      icon="📡"
      title="Radar"
      description="Research feed — Reddit, YouTube, newsletters aggregated"
    />
  );
}
